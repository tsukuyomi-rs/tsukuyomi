[package]
name = "tsukuyomi-internal"
description = "internal crate for Tsukuyomi."
version = "0.4.2" # <cargo-version-sync>
authors = ["Yusuke Sasaki <yusuke.sasaki.nuem@gmail.com>"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/tsukuyomi-rs/tsukuyomi.git"

build = "build.rs"

[dependencies]
failure = "0.1.3"
indexmap = "1"

[features]
localmap = []
