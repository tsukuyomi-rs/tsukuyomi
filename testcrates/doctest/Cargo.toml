[package]
name = "doctest"
version = "0.0.0"
edition = "2018"
authors = ["Yusuke Sasaki <yusuke.sasaki.nuem@gmail.com>"]
publish = false

[lib]
name = "doctest"
path = "lib.rs"

[dependencies]
doubter = { version = "0.1.0", default-features = false }

[dev-dependencies]
tsukuyomi = { path = "../../tsukuyomi" }
tsukuyomi-server = { path = "../../tsukuyomi-server" }
