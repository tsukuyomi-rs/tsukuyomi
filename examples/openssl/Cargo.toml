[package]
name = "example-openssl"
version = "0.0.0"
edition = "2018"
authors = ["Yusuke Sasaki <yusuke.sasaki.nuem@gmail.com>"]
publish = false

[[bin]]
name = "example_openssl"
path = "src/main.rs"
doc = false

[dependencies]
tsukuyomi = { version = "0.5.0-dev" }
tsukuyomi-server = { version = "0.2", features = ["use-openssl"] }
openssl = "0.10"
